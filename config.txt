### Gnome Setup ###
git config --global user.email "barsukov.m23@gmail.com"
git config --global user.name "Michael Barsukov"

# System Fonts
Intf: Noto Sans R        10
Docs: Times New Roman    10
Mono: Saarasa Mono CL R  10
WinT: Noto Sans R        10

Hinting: Full
Antialiasing: Standard

# Ubuntu-based
  sudo update-alternatives --config x-terminal-emulator

# Manjaro
  sudo pacman -Rdd manjaro-gdm-theme manjaro-gdm-branding


### Packages ###
alacritty
bluez (Arch)
catimg
cmatrix
code
  sudo snap install code-insiders --classic
flatpak
gnome-session gnome-shell
make gcc python python2 base-devel
mlocate
npm
python-pip
ranger (or nnn)
telegram-desktop
tmux
  tmux source-file ~/.tmux.conf \;
ueberzug
  pip3 install ueberzug
vim
w3m
zsh

# Fonts
fonts-noto
ms-fonts (Arch):
  AUR: ttf-ms-fonts
ms-fonts (Ubuntu):
  sudo add-apt-repository multiverse
  ttf-mscorefonts-installer
  sudo fc-cache -f -v

# Oh-My-ZSH
sh -c "$(wget https://raw.github.com/ohmyzsh/ohmyzsh/master/tools/install.sh -O -)"
