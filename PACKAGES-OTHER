GIMP
$flatpak install org.gimp.GIMP

FontManager
$ flatpak install org.gnome.FontManager

EmojiOneColor
$ wget https://github.com/eosrei/emojione-color-font/releases/download/v1.4/EmojiOneColor-SVGinOT-Linux-1.4.tar.gz && tar zxf EmojiOneColor-SVGinOT-Linux-1.4.tar.gz && cd EmojiOneColor-SVGinOT-Linux-1.4
$ ./install

NVIDIA Drivers
$ sudo pacman -Syu
$ uname -r
$ sudo pacman -S dkms linuxXX-headers ; XX - kernel version
/etc/default/grub:
GRUB_CMDLINE_LINUX="nouveau.modeset=0"
$ sudo update-grub
$ sudo sh ./NVIDIA-Linux-x86_64-XXX.run
Yes. Yes.

NodeJS
curl -sL install-node.now.sh/release | bash
