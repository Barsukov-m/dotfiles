# Searching
1. (Prefix)+[ -- copy mode
2. Ctrl+S -- to search

# Window Manipulations
(Prefix)+{ -- move left
(Prefix)+} -- move right
(Prefix)+o -- swap
(Prefix)+z -- toggle zoom
(Pref

# Accessibility
Alt+. -- last word typed
